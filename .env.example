# 服务器配置
PORT=3000
# HTTP服务器监听地址
# 0.0.0.0 - 允许所有IP访问（部署到服务器时使用）
# 127.0.0.1 或 localhost - 仅本地访问（开发环境使用）
HOST=0.0.0.0

# 环境配置
NODE_ENV=production
LOG_LEVEL=info

# 以下配置项现在通过数据库管理，不再需要在.env中配置
# 请访问 http://服务器IP:3000/admin 进行可视化配置

# Telegram Bot Token - 从 @BotFather 获取
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# OpenAI API 配置
# OPENAI_API_KEY=your_openai_api_key
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o

# 币安API配置（可选）
# BINANCE_API_KEY=your_binance_api_key
# BINANCE_SECRET=your_binance_secret